# aws-week-by-week

Weekly proof-of-work repo for an 8-week AWS build-up: each `wNN/` folder is one week's deployable artifact, config, and a short README of what was built and why.

- [w02/](w02/) — EC2 properly: Elastic IP, locked-down security group, FastAPI app run via systemd, nginx reverse proxy.
